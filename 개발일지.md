# GVCS 자치부서 홈페이지 — 개발 일지 (상세)

- 대상: `gvcs-autonomy-homepage` (React 19 + Vite)
- 저장소: `github.com/mg223080612-a11y/Dormhome` (브랜치 `main`)
- 서비스 주소: `https://sos.gvcs.kr`
- 작성 기준: 대화 기록 + 프로젝트 파일 수정 시각(KST)

---

## 0. 전체 요약

| 날짜 | 시간대 | 소요 | 핵심 작업 | 상태 |
|---|---|---|---|---|
| 2026-06-28 (일) | 16:13 ~ 22:49 | 약 6시간 30분 | 프로젝트 초기 구축 · 첫 빌드 | 완료 |
| 2026-06-29 (월) | 09:11 | — | GitHub 원격 저장소 연결 | 완료 |
| 2026-08-04 (화) | 14:06 ~ 14:18 | 약 15분 | 홈 대시보드 · SOS 로고 | 완료 |
| 2026-08-20 (목) | 23:10 | — | 사이드바 메뉴 구성 수정 | 완료 |
| 2026-08-27 (수) | 08:20 ~ 08:41 | 약 20분 | 레이아웃 · 목업 데이터 · 관리자 페이지 | 완료 |
| 2026-09-15 (화) | 20:00 ~ 22:06 | 약 2시간 5분 | 인증 · 메뉴 · 디자인 · 라우팅 · 배포 | 완료 |
| 2026-09-18 (금) | 19:40 ~ 19:58 | 약 20분 | 달력 UI · 인수인계 문서 | 완료 |

누적 작업 시간 **약 9시간 30분**

> 6~8월 구간은 대화 기록이 남아 있지 않아 파일 수정 시각에서 역산한 추정치입니다.
> 9월 두 세션은 대화 기록을 그대로 따랐습니다.

---

# 1. 2026-09-15 (화) 20:00 ~ 22:06 · 약 2시간 5분

이날의 목표는 **"학교 계정만 쓸 수 있는 사이트로 만들고, 실제 배포 가능한 상태까지 정리하기"** 였습니다.
아래는 요청 순서대로의 기록입니다.

---

## 1-1. 로그인을 학교 계정으로만 제한 (20:00 ~ 20:10)

**요청** — "@gvcs-mg.org 계정만 등록되도록"

**확인한 기존 상태**

- Firebase 구글 로그인은 이미 붙어 있었고, 허용 도메인은 `.env` 의 `VITE_ALLOWED_EMAIL_DOMAIN` 값으로 읽고 있었음
- 그런데 프로젝트 루트에 `.env` 파일 자체가 없었음
- 코드를 열어 보니 다음과 같은 분기가 있었음

  ```js
  export const isAllowedEmail = (email) =>
    !ALLOWED_DOMAIN || domainOf(email) === ALLOWED_DOMAIN;   // 값이 비면 전부 통과
  ```

  즉 **설정이 비어 있는 그 상태에서는 아무 구글 계정이나 로그인이 되는 상황**이었습니다.
  설정 실수 하나가 곧 전체 공개로 이어지는 구조라 판단해, 단순히 값만 채우지 않고 구조를 바꿨습니다.

**작업 내용**

| 파일 | 변경 |
|---|---|
| `src/lib/firebase.js` | `DEFAULT_ALLOWED_DOMAIN = 'gvcs-mg.org'` 상수 추가. 환경변수가 비어도 기본값이 적용되도록 변경. 앞의 `@` 를 붙여 써도 인식하도록 정규화. 구글 계정 선택창 힌트(`hd`)를 항상 적용 |
| `src/utils/auth.js` | 빈 값 통과 분기 제거 → `domainOf(email) === ALLOWED_DOMAIN` 만 통과. 오류 문구를 `@gvcs-mg.org 계정으로만…` 으로 수정 |
| `src/components/Login.jsx` | 로그인 화면 안내 문구를 도메인 제한 안내로 통일 |
| `.env.example` | 허용 도메인 기본값과 설명 갱신 |
| `README.md` | 도메인 제한 동작 설명 추가 |

**동작** — 허용되지 않은 계정은 로그인 팝업 직후 자동 로그아웃되고 안내 문구가 표시됩니다.
로그인 상태 구독(`onAuthStateChanged`)에도 같은 검사를 두어, 이미 로그인돼 있던 외부 계정도 새로고침 시 풀립니다.

**한계로 남긴 부분** — 이 검증은 프런트엔드에만 있어 우회가 가능합니다. 데이터를 서버로 옮길 때
서버에서도 이메일 도메인을 검사해야 한다는 점을 기록해 두었습니다.

---

## 1-2. Firebase 설정값 전달 (20:10 ~ 20:25)

**요청** — Firebase 웹 config 를 전달받음

**진행**

1. 전달받은 값으로 `.env` 내용을 구성 → **원격 도구가 `.env` 파일 쓰기를 허용하지 않아 직접 저장 실패**
2. `env-복사용.txt` 로 대신 저장하고 복사 방법 안내 (VS Code 이용 / PowerShell `Move-Item`)
3. 그럼에도 `.env` 가 없어 `auth/invalid-api-key` 오류가 계속 발생

**결정** — Firebase 설정값을 `src/lib/firebase.js` 의 `DEFAULT_CONFIG` 로 코드에 내장하고,
`.env` 값이 있으면 그쪽을 우선하도록 했습니다.

판단 근거:

- Firebase 웹 config 는 비밀값이 아닙니다. 어차피 빌드 결과물에 포함되어 브라우저로 전달됩니다.
  실제 보호는 Firebase 콘솔의 *승인된 도메인* 과 보안 규칙이 담당합니다.
- `.env` 는 git 에 올라가지 않으므로, 다른 PC 에서 clone 할 때마다 같은 오류가 재발합니다.
  기본값이 있으면 받자마자 실행됩니다.

**부수 작업** — `firebase/auth` import 실패 오류가 떠서 확인한 결과 `node_modules/firebase` 가 없었습니다.
`package.json` 에는 의존성이 있었으나 설치가 안 된 상태여서 `npm install` 로 해결했습니다.

---

## 1-3. 사이드바 메뉴를 4개 카테고리로 개편 (20:10 ~ 20:15)

**요청** — "이 메뉴들을 뒤에 4카테고리 안에 넣고싶어" (메뉴 스크린샷 첨부)

**확인한 기존 상태** — 상단 4개(Home / Surveys / Policy Progress / Events) 아래에
급식·달력·주별말씀·택시메이트·건의함·수리요청·희망메뉴 2종·사진첩·마켓 10개가 한 줄로 나열되어 있었음

**선택지 확인** — 카테고리를 새로 만들지, 기존 4개를 쓸지 / 아코디언인지 항상 펼침인지 확인 후
**기존 4개 + 클릭 시 펼쳐지는 아코디언** 으로 결정

**최종 분류**

```
Home            ├ 달력 · 주별 말씀 · 사진첩
Surveys         ├ 건의함 · 희망메뉴/급식 · 희망메뉴/매점
Policy Progress ├ 수리 요청
Events          ├ 급식 · 택시메이트 · 마켓
```

**작업 내용**

| 파일 | 변경 |
|---|---|
| `src/data/menu.js` | `navTree`(카테고리 → `children`) 구조 신설. 기존 `primaryMenu`/`secondaryMenu`/`menu` 는 여기서 파생시켜 하위 호환 유지. 현재 페이지의 상위 카테고리를 찾는 `parentOf()` 추가 |
| `src/components/Sidebar.jsx` | 아코디언 렌더링. 카테고리 이름 클릭 = 페이지 이동 + 펼침, 오른쪽 꺾쇠(›) 클릭 = 이동 없이 접기/펴기. 현재 페이지가 속한 카테고리는 자동 펼침 |
| `src/styles/layout.css` | `.side-group` / `.side-row` / `.side-toggle` / `.side-sub` / `.side-child` 스타일 추가 |

**설계 의도** — 이동과 펼침을 한 버튼에 묶으면 "목록만 확인하고 싶은" 경우에도 페이지가 바뀝니다.
두 동작을 분리해 둘 다 자연스럽게 되도록 했습니다.
분류 변경은 `menu.js` 의 `children` 배열만 옮기면 되도록 한 곳에 모았습니다.

---

## 1-4. 디자인 개편 — SOS 포스터 콘셉트 (20:30 ~ 21:00)

이 작업은 여러 번 방향을 조정하며 진행됐습니다. 순서대로 적습니다.

**① 색 적용** — "홈페이지를 이런 색으로 배경해줘" (SOS 포스터 이미지)

- 포스터에서 노랑 `#f2b02c`, 빨강 `#c8172b`, 크림 `#f6eddc` 를 뽑아 `theme.css` 상단에 SOS 팔레트 토큰으로 정리
- 페이지 배경 전체를 노랑으로, 카드·사이드바는 흰색 유지, 히어로 제목을 빨강으로 변경

**② 범위 축소** — "전체를 바꾸지말고 약간 세련되게, 이 배경이미지를 사용해서. 딱 저만큼만 구겨진 모양, 나머진 흰색으로"

- 전체 노랑을 되돌리고, 구겨진 종이 질감은 홈 상단 히어로 영역에만 적용
- 배경 이미지를 `public/paper-yellow.jpg` 로 최적화 저장 (1.2MB → 154KB)
- 히어로 높이 축소, 아래쪽 가장자리에만 은은한 그림자를 넣어 흰 본문과 이어지게 처리

**③ 요소 합성** — 찢어진 메모지·느낌표 이미지를 받아 포스터처럼 겹쳐 배치

- 두 이미지의 흰 배경을 제거해 투명 PNG 로 변환 (`note-paper.png`, `hero-marks.png`)
- 구조: 노란 배경 → 메모지(가운데) → 느낌표(양옆) 3겹
- **문제** — 느낌표 체크무늬 안쪽 흰 칸까지 투명해짐
  → 바깥 배경과 이어진 흰색만 제거하도록 방식 변경(연결 성분 판별)해 무늬 복원

**④ 레이아웃 붕괴와 원인 규명** — "화면 깨졌어 다시 확인해"

- 브라우저로 직접 접속해 화면을 확인하고, 실제 요소 크기를 측정
- **원인** — 메모지 위에 올린 `public/sos-mark.svg` 가 **2.6MB 짜리 초대형 SVG** 여서
  지정한 비율(`width: 72%` 등)이 먹지 않고 컨테이너를 밀어냄

**⑤ 단순화** — "그냥 이걸 써줘" (완성된 합성 이미지 전달)

- 3겹 합성을 폐기하고 완성 배너 이미지 한 장으로 교체 → 레이아웃 문제 해소
- 구조가 `배너 이미지 + 그 아래 말씀` 두 덩어리로 단순해짐

**⑥ 크기 조정** — "밑에가 잘리지 않도록" → "너무 확대된 느낌이야" → 가로로 긴 배너로 재교체

- 잘림 방지를 위해 `object-fit: cover` 와 `max-height` 제거, 원본 비율 유지
- 폭을 제한해 확대감을 줄였다가, 최종적으로 가로형 배너(`hero-banner.jpg`, 1357×476)를 받아
  콘텐츠 폭 전체에 배치

**⑦ 카드 색 조정** — "주별말씀 네모칸 파란색 배경을 흰 배경에 검은 글씨로"

- `.verse-card` 를 남색 그라데이션 → 흰 배경 + 검은 글씨 + 옅은 테두리·그림자로 변경해 다른 카드와 통일

**남긴 기록** — `note-paper.png` 에는 스톡 사이트 워터마크가 있어 재사용하지 않도록 문서화했습니다.
현재 미사용 자산: `hero-sos.jpg`, `note-paper.png`, `hero-marks.png`, `paper-yellow.jpg`

---

## 1-5. 주소창에서 `#` 제거 (20:40 ~ 20:45)

**요청** — "도메인 사이에 #를 없앨 수 있어?" (`localhost:5173/#/calendar` 스크린샷)

**작업 내용** (`src/App.jsx`)

- 해시 기반 라우팅을 History API 방식으로 전환
  - 경로 ↔ 페이지 변환 함수 `pathToPage` / `pageToPath` 추가
  - 이동은 `history.pushState`, 뒤로/앞으로는 `popstate` 구독으로 처리
  - 페이지 이동 시 스크롤을 상단으로 이동
- 기존 `#/xxx` 링크로 들어와도 첫 렌더에서 `replaceState` 로 깔끔한 주소로 정리
- 로그아웃 시 주소 처리도 같은 함수를 쓰도록 통일

**주의로 남긴 점** — 배포 시 모든 경로를 `index.html` 로 보내는 rewrite 가 없으면
`/calendar` 직접 접속이 404 가 됩니다. 개발 서버는 기본 지원하지만 배포처에서는 설정이 필요합니다.
(Firebase Hosting `rewrites` / Cloudflare Pages `_redirects`)

라우터 라이브러리는 도입하지 않았습니다. 페이지가 단순 매핑이라 의존성을 늘릴 이유가 없다고 판단했습니다.

---

## 1-6. 주별 말씀을 관리자 페이지와 연결 (20:38 ~ 20:42)

**요청** — "주별말씀은 admin페이지에서 업데이트 하면 화면에 표시되는 형식으로"

**원인 파악** — 관리자 페이지에는 주별 말씀 탭이 이미 있었고 저장도 되고 있었으나,
정작 주별 말씀 페이지는 `mockData.js` 의 고정값을 직접 참조하고 있어 수정이 반영되지 않았습니다.

**작업 내용**

| 파일 | 변경 |
|---|---|
| `src/utils/storage.js` | 저장 시 `window.dispatchEvent(new CustomEvent('storage-write'))` 발생. 브라우저 기본 `storage` 이벤트는 *다른 탭에서만* 발생하기 때문에 같은 탭 갱신용 신호를 직접 만듦 |
| `src/utils/useStoredValue.js` (신규) | `storage-write`(같은 탭)와 `storage`(다른 탭)를 모두 구독해 즉시 재렌더하는 훅 |
| `src/pages/FeaturePages.jsx` | `VersePage` 가 훅으로 `admin-verse` 를 읽도록 변경, 마지막 업데이트 시각 표시 추가 |
| `src/pages/AdminPage.jsx` | 타이핑마다 저장하던 방식 → **저장 버튼** 방식으로 변경. "저장되었습니다" 표시, 기본값 복원 버튼, `updatedAt` 기록 추가 |
| `src/styles/pages.css` | 저장 버튼·안내 문구·업데이트 시각 스타일 |

**추가 반영** — 이후 홈 화면 요청("홈화면에 주별말씀되어있는 칸에 주별말씀 업데이트 되도록")에 따라
`HomeHero.jsx` 도 같은 훅으로 말씀을 읽도록 해, 관리자에서 저장하면 홈 배너 아래에도 즉시 반영되게 했습니다.

**설계 메모** — 새로운 데이터를 추가할 때도 이 흐름(저장 → 이벤트 → 구독 훅)을 따르도록 문서에 남겼습니다.

---

## 1-5b. Cloudflare 백엔드 설계 (20:45 ~ 20:50)

**질문** — "db를 firebase랑 cloudflare 2개를 함께 써도 되는거야?" → "이제 모든 데이터 저장은 cloudflare에,
호스팅만 firebase로"

**결정 사항** (선택지 확인 후)

- 저장소: **Cloudflare D1** (관계형, 목록 데이터 조회·정렬에 적합)
- 인증: **Firebase 구글 로그인 유지**, Worker 가 토큰을 검증
- 범위: 주별말씀 · 급식 · 공약 · 설문 먼저 이관

**작성한 것** (`worker/` 폴더)

| 파일 | 내용 |
|---|---|
| `worker/wrangler.toml` | D1 바인딩, `FIREBASE_PROJECT_ID`, `ALLOWED_DOMAIN`, `ALLOWED_ORIGINS` |
| `worker/schema.sql` | `docs`(주별말씀·급식을 JSON 으로) / `pledges` / `surveys` 테이블 + 초기 데이터 |
| `worker/src/index.js` | REST API. GET 은 공개, 쓰기는 Firebase ID 토큰 검증 후 학교 도메인만 허용. CORS 처리 |
| `worker/package.json` | wrangler 스크립트 (dev / deploy / db:init) |

**기술 선택 이유** — Worker 런타임에서는 `firebase-admin` 을 쓸 수 없어, 구글 공개키(JWKS)를 받아
WebCrypto 로 토큰 서명을 직접 검증하도록 구현했습니다.
통째로 다루는 데이터는 문서형 테이블에, 목록형은 별도 테이블에 두어 다루기 쉽게 나눴습니다.

**현재 상태** — 프런트엔드 연결 전 단계에서 **보류**. 남은 단계는 문서(`CLAUDE.md`)에 5단계로 정리해 두었습니다.

---

## 1-7. 배포 준비와 장애 대응 (21:00 ~ 22:06)

**① 배포 설정 작성**

- `firebase.json` — SPA rewrite(`** → /index.html`), `index.html` 은 캐시 금지 / `assets/` 는 장기 캐시
- `.firebaserc` — 기본 프로젝트 `dormhome-1c840`
- 캐시 정책을 명시한 이유: 배포할 때마다 옛 번들이 계속 보이는 문제를 구조적으로 막기 위해

**② 반복된 배포 오류 — 원인 규명에 가장 오래 걸린 건**

증상: 빌드·배포를 해도 콘솔에 계속 `auth/invalid-api-key` 와
`[firebase] .env 설정이 비어 있습니다` 오류가 떴습니다.

추적 과정:

1. 오류 문구가 **수정 전 코드에만 있던 문장**임을 확인 → 옛 번들을 보고 있다고 판단
2. 빌드 결과물을 직접 열어 분석 → 새 배너 이미지 경로는 들어 있는데 firebase 부분만 옛 코드
3. 소스 파일을 다시 확인 → `src/lib/firebase.js` 가 **절반만 저장된 상태**
   (앞부분 주석은 새 내용, 정작 설정값 블록은 옛 내용)
   두 번에 나눠 저장한 수정 중 뒤쪽이 파일에 반영되지 않은 것이 원인
4. 올바른 내용으로 덮어쓰고, **파일을 다시 읽어 내용까지 대조**해 확인

교훈 — 이후 모든 수정은 저장 후 파일을 다시 읽어 확인하고, 화면까지 열어 검증하는 방식으로 진행했습니다.

**③ 서비스 주소 정리**

- 최종적으로 서비스 주소를 `https://sos.gvcs.kr` 로 정리
- 기존 Firebase Hosting 주소는 `public-redirect/` 안내 페이지와 301 리다이렉트로 전환
  (`firebase.json` 의 `public` 을 `public-redirect` 로, `rewrites` 대신 `redirects` 사용)
- 배너 좌측에 남아 있던 스프링 노트 천공 부분을 CSS 로 크롭 보정

**④ 배포 계정 안내** — Firebase CLI 계정 변경 절차(`firebase logout` → `firebase login` →
`firebase login:list` → `firebase projects:list`)와 프로젝트 권한 확인 방법 안내

---

# 2. 2026-09-18 (금) 19:40 ~ 19:58 · 약 20분

## 2-1. 프로젝트 폴더 접근 문제 (19:40 ~ 19:48)

작업을 시작하려는데 프로젝트 폴더를 읽을 수 없었습니다. 연결 목록에는 경로가 있는데 실제로는
존재하지 않는 상태여서, 드라이브 연결 문제로 판단하고 상황을 알린 뒤 재연결을 기다렸습니다.
재연결 후 정상적으로 작업을 재개했습니다.

## 2-2. 달력 UI 개선 (19:49)

**요청** — "달력에서 다른 달로 옮겼을때 '오늘' 버튼박스의 자리가 애매함. 그냥 오늘로 가는게 사라지고
오늘 표시되는 걸로, 새로고침하면 초기화되게"

**확인한 기존 동작** — 이번 달이 아닐 때만 "오늘" 버튼이 조건부로 나타나는 구조라,
달을 옮길 때마다 버튼이 생겼다 사라지며 헤더(‹ 2026년 9월 ›)가 흔들렸습니다.

**작업 내용**

| 파일 | 변경 |
|---|---|
| `src/pages/FeaturePages.jsx` | `MonthCalendar` 의 조건부 "오늘" 버튼 제거. 헤더는 이전/다음 버튼과 제목만 남김 |
| `src/styles/pages.css` | 더 이상 쓰지 않는 `.calendar-today-btn` 스타일 제거 |

- 오늘 날짜는 기존 `.calendar-day.today` 강조(파란 배경 + 날짜 숫자 파란 박스)로만 구분
- 보고 있는 달은 컴포넌트 상태로만 관리되므로 **새로고침하면 항상 이번 달로 초기화**됩니다
  (별도 저장을 하지 않는 기존 구조를 확인하고 그대로 유지)

## 2-3. 인수인계 문서 작성 (19:58)

Claude Code 등 다른 환경에서 이어서 작업할 수 있도록 `CLAUDE.md` 를 작성했습니다.
프로젝트 개요·파일 지도·기술 결정과 그 이유·알려진 문제·다음 작업·코드 컨벤션·배포 체크리스트 10개 장으로 구성했습니다.
관리자 PIN 같은 민감한 문자열은 값을 적지 않고 "코드에 하드코딩되어 있으니 서버 기반으로 옮겨야 한다"고만 기록했습니다.

---

# 3. 이번 기간에 해결한 문제 정리

| # | 문제 | 원인 | 해결 | 소요 |
|---|---|---|---|---|
| 1 | 설정이 비면 아무 구글 계정이나 로그인됨 | 도메인 값이 비었을 때 전체 허용하는 분기 | 기본값 상수 고정 + 빈 값 통과 분기 제거 (fail-closed) | 10분 |
| 2 | `firebase/auth` import 실패 | `package.json` 에는 있으나 패키지 미설치 | `npm install` | 5분 |
| 3 | `.env` 파일을 만들 수 없음 | 도구가 `.env` 쓰기를 차단 | 설정값을 코드 기본값으로 내장 (웹 config 는 공개값) | 15분 |
| 4 | 느낌표 무늬 속 흰색이 사라짐 | 흰색 전체를 투명 처리 | 바깥 배경과 이어진 흰색만 제거하도록 변경 | 10분 |
| 5 | 홈 화면 레이아웃 붕괴 | 2.6MB SVG 로고가 크기 지정을 무시하고 컨테이너를 밀어냄 | 완성 배너 이미지 한 장으로 구조 단순화 | 20분 |
| 6 | 관리자 수정이 화면에 반영 안 됨 | 화면이 고정 데이터를 직접 참조 | 저장 이벤트 + 구독 훅 도입 | 15분 |
| 7 | 배포 후에도 옛 오류가 계속 발생 | 설정 파일이 절반만 저장됨 | 파일 복구 + 내용 대조 검증, 이후 모든 수정에 검증 절차 적용 | 30분 |
| 8 | 달력 상단 영역이 흔들림 | 조건부로 나타나는 "오늘" 버튼 | 버튼 제거, 날짜 칸 강조 표시로 대체 | 5분 |

---

# 4. 이번 기간 변경 파일 목록

**신규**

```
src/utils/useStoredValue.js      저장값 즉시 반영 훅
worker/wrangler.toml             Cloudflare Worker 설정
worker/schema.sql                D1 스키마
worker/src/index.js              API 본체
worker/package.json
firebase.json / .firebaserc      배포 설정
public-redirect/index.html       구주소 안내 페이지
public/hero-banner.jpg           홈 배너 (현재 사용)
public/hero-sos.jpg              이전 배너
public/paper-yellow.jpg          종이 질감
public/note-paper.png            메모지 컷아웃
public/hero-marks.png            느낌표 컷아웃
CLAUDE.md                        인수인계 문서
개발일지.md                       이 문서
```

**수정**

```
src/App.jsx                      해시 → History API 라우팅
src/lib/firebase.js              설정 기본값 내장, 도메인 fail-closed
src/utils/auth.js                도메인 검증 강화
src/utils/storage.js             저장 이벤트 발생
src/components/Login.jsx         안내 문구
src/components/Sidebar.jsx       4카테고리 아코디언
src/components/HomeHero.jsx      배너 + 주별 말씀
src/data/menu.js                 navTree 구조
src/pages/AdminPage.jsx          주별 말씀 저장 UI
src/pages/FeaturePages.jsx       주별 말씀 연동, 달력 버튼 제거
src/styles/theme.css             SOS 팔레트 토큰
src/styles/layout.css            사이드바·히어로
src/styles/pages.css             말씀 카드, 달력
src/styles/login.css             로그인 배경
.env.example / .gitignore / README.md
```

---

# 5. 남은 과제

| 우선순위 | 과제 | 배경 |
|---|---|---|
| 높음 | 데이터를 Cloudflare D1 으로 이관 | 현재 `localStorage` 라 관리자가 저장한 내용이 그 브라우저에서만 보임. 백엔드 뼈대는 작성 완료, 프런트 연결만 남음 |
| 높음 | 관리자 권한을 계정 기반으로 전환 | PIN 이 코드에 하드코딩되어 번들에 노출됨 |
| 중간 | 서버 측 도메인 검증 추가 | 현재 검증이 프런트엔드에만 있어 우회 가능 (Worker 코드에는 이미 포함) |
| 중간 | `sos-mark.svg` 경량화 | 2.6MB — 첫 로딩 지연, 과거 레이아웃 사고의 원인 |
| 낮음 | 미사용 자산·임시 파일 정리 | `hero-sos.jpg`, `note-paper.png`, `hero-marks.png`, `paper-yellow.jpg`, `env-복사용.txt`, `Claude outputs/` |
