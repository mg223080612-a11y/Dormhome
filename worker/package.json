{
  "name": "gvcs-api",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute gvcs-db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute gvcs-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
